# Lint for the guide. Rules live in .vale/styles/Guide; the word budget is a
# separate check, bin/lint-guide-budget.
StylesPath = .vale/styles
MinAlertLevel = suggestion

[guide/*.md]
BasedOnStyles = Guide

[.vale/fixtures/*.md]
BasedOnStyles = Guide
